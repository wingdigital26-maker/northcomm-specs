# loop-log.md — northcomm / site
| round | capture | mechanical gate | judge A | judge C | better? | fix scope | note |
|---|---|---|---|---|---|---|---|
| 2 | desktop.png, mobile.png, desktop-nojs.png, result-desktop.png, result-mobile.png in .visual/round-2 | PASS (visual_gate.py --round 2; lint WARN-only: uniform-radius, expected per pinned 0/2px/50% scale) | n/a (builder round, no decider pass this round) | n/a | yes vs round-1 tile A: hero now fills first viewport w/ real report sheet, eyebrow labels removed, two-col Notes/Request band, result card restyled to sheet system, footer tidied, 404 paths made depth-agnostic | index.html, 404.html, privacy.html, support.html, design-brief.md | Rebuilt presentation per Decision (tile A layout + real report-sample.webp hero artifact). Kept all JS behaviour byte-identical (Supabase lookup, esc(), popup-safe PDF open, FAQ toggles, deep-link). Full-page screenshot of the post-lookup result shows a duplicated sticky header — confirmed via a non-full-page viewport screenshot that this is a Playwright/Chromium full-page-capture + position:sticky stitching artifact, not a real rendering bug (header renders once, correctly, during normal scroll). No browse/list-of-all-reports section was built: README's hard constraint ("no browse of all reports, never re-add") overrides the task brief's assumption that one already existed — none was present in index.html to begin with. Fixed support.html's stale "Browsing every report" section that described a feature the page never had.
| 3 | desktop.png, mobile.png, desktop-nojs.png in .visual/round-3; result-desktop.png (1440x900 viewport, not full-page) + result-mobile.png (390x844 viewport, not full-page) in .visual/round-3 | PASS (visual_gate.py --scope index.html,loop-log.md --prev .visual/round-2 --fix-round --round 3; change-proof: desktop 6.19% / mobile 10.04% pixels changed vs round-2, bbox covers the hero+result, zero_columns=0 on both -- change confirmed landed) | n/a (scoped critic fix round) | n/a | yes: hero dead space closed (padding capped at 96px top/bottom, grid restored faint + fading), report sheet column widened to 600px at 1440, arrow removed from "View sample" (now "Run this lookup"), result card no longer hidden under the sticky header (scroll-margin-top:74px), mobile no longer renders the 7MB PDF iframe (verified scrollWidth==innerWidth at 375 and 390, before and after lookup) | index.html, loop-log.md | Scoped fix round addressing all 6 critic defects: (1) hero padding clamp(32px,5vw,96px), grid-template-columns 1fr minmax(480px,600px), restored `.hero::before` blueprint grid (34px cells, mask-image fading to transparent at 72%); (2) sheet-cap wording "Run this lookup", &darr; glyph removed; (3) `.result{scroll-margin-top:74px}` (58px header + 16px); (4) `renderFound()` now checks `window.innerWidth > 760` and skips the iframe entirely on narrow viewports (shows summary+buttons+"Opens in a new tab" note instead), desktop iframe capped `height:min(70vh,900px)` and the loading state is now a quiet 1px hairline frame on --sheet, not a grey slab; (5) `body.has-result` class toggled in show()/reset(), `.hero-sample` hidden under it below 920px; (6) confirmed no horizontal overflow at 375/390 via `document.documentElement.scrollWidth === window.innerWidth`, checked both before and after the lookup runs. "Open full report" href/window.open logic untouched.
| 4 | desktop.png, mobile.png, desktop-nojs.png in .visual/round-4 (plain-page capture); result-desktop.png/result-mobile.png (viewport, 1440x900 and 390x844, post-lookup) + afternew-desktop.png/afternew-mobile.png (post "New lookup" click) in .visual/round-4 | PASS (visual_gate.py --scope index.html,loop-log.md --prev .visual/round-3 --round 4, WITHOUT --fix-round: the plain-page capture is pixel-identical to round-3 by design -- every round-4 change only affects the has-result/result-card state, which the gate's automated capture never triggers, so --fix-round's zero-diff check would false-fail; change-proof still ran informationally and correctly showed 0 changed pixels on the plain page, confirmed real via the separate result-state screenshots instead) | n/a (scoped critic fix round, final/capped) | n/a | yes: result state no longer double-shows the report (hero collapses to a compact console-only bar and the sample sheet hides on every viewport once body.has-result is set); result card now spans the full 1080px width with a real two-column desktop layout (320px left: serial/title/chip/specs/buttons, right: embed); serials render "NC-121484" everywhere instead of "NC  -  121484"; loading affordance moved to the top third of the embed frame; "New lookup" fully restores the hero and refocuses the input (proven in afternew-*.png) | index.html, loop-log.md | (1) `body.has-result .hero .grid{grid-template-columns:1fr;min-height:0}` + hides `.hero h1/.lede/.hero-sample` on ALL viewports (previously only <920px); `.result` widens to max-width:1080px under has-result; (2) `renderFound()` restructured into `.card-left`/`.card-right`, `.card.has-embed{display:grid;grid-template-columns:320px 1fr}` at >760px (single column, no grid, at <=760px, unchanged from round 3); buttons + large-file note now live in card-left with the specs; (3) added `dispSerial()` helper (`trim().replace(/\s*-\s*/g,"-")`) used everywhere a serial is PRINTED (result head, not-found message) -- matching (`tidy()`), the Supabase filter and the PDF path/URL all still use the raw, unnormalized value; (4) `.pdfloading` changed from centered to `align-items:flex-start;padding-top:10%` so "Loading report" sits in the top third of the min(70vh,900px) frame; (5) `.card .head .ser` bumped 15px->20px per "large mono" spec. Verified with viewport (non full-page) screenshots post-lookup at both sizes, plus a post-"New lookup" screenshot at both sizes proving the hero and sample sheet return and the input refocuses.
